get:
  tags:
    - person
  summary: Get a list of persons matching the search params
  parameters:
    - name: norEduPersonNIN
      in: query
      schema:
        type: array
        items:
          type: string
    - name: id
      in: query
      schema:
        type: array
        items:
          type: string
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../Person.yaml#/Person"
